---
description: Implements the active iGrocery plan from `tasks/current.md`.
---

# Implement Task

Delegate to `igrocery-developer`.

**Scope:** $ARGUMENTS

If no scope is provided:

- implement all open steps from `tasks/current.md`

If a scope is provided:

- implement only the matching steps

Update `tasks/current.md` in-place:

- checkboxes
- short progress block at the end
